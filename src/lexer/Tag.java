package lexer;

public enum Tag {
    IF, ELSE, WHILE, INT, FLOAT,
    PLUS, MINUS, TIMES, OVER, ASSIGN,
    LT, GT, LE, GE, EQ, NE,
    LBracket, RBracket, SEMICOLON, Quotation,
    ID, NUMBER, ERROR
}
